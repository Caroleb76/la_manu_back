model ExtraCost {
  id             String       @id @default(uuid())
  category       String
  val            String?



  files File[]
  interventions Intervention[] @relation("extraCostIntervention")
  @@map("ExtraCost") // Optional: match DB table name if needed
}

